import { EarnClient } from '@/components/client/dashboard/EarnClient'

export const metadata = {
  title: 'Earn - JobbaWorks',
  description: 'Earn money by completing daily reads, tasks, and promotions.',
}

export default function EarnPage() {
  return <EarnClient />
}
